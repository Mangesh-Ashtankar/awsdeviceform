version: 0.2

phases:
  build:
    commands:
      - chmod +x ./gradlew
      - ./gradlew assembleDebug
  post_build:
     commands:
      - find / "*.apk"
artifacts:
  files:
     - './awsdevicefarm'
  discard-paths: yes
